import { CutomepipePipe } from './cutomepipe.pipe';

describe('CutomepipePipe', () => {
  it('create an instance', () => {
    const pipe = new CutomepipePipe();
    expect(pipe).toBeTruthy();
  });
});
